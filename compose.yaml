# Comments are provided throughout this file to help you get started.
# If you need more help, visit the Docker Compose reference guide at
# https://docs.docker.com/go/compose-spec-reference/

# Here the instructions define your application as a service called "server".
# This service is built from the Dockerfile in the current directory.
# You can add other services your application may depend on here, such as a
# database or a cache. For examples, see the Awesome Compose repository:
# https://github.com/docker/awesome-compose
name: TransAeroHub

services:
  redis:
    container_name: redis-dev
    ports:
      - "6379:6379"
      - "8001:8001"
    restart: on-failure
    networks:
      - superfolio-net
    logging:
      options:
        max-size: 1g
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping | grep PONG"] # ["CMD", "redis-cli","ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    environment:
      - REDIS_ARGS=--save 60 1000 --appendonly yes --loglevel warning
      - REDISTIMESERIES_ARGS=RETENTION_POLICY=20
    image: redis/redis-stack:6.2.6-v17 # redis/redis-stack-server:6.2.6-v17

  superfolio:
    container_name: superfolio
    build:
      context: ./superfolio
    ports:
      - "8080:8080"
    restart: on-failure
    networks:
      - superfolio-net
    logging:
      options:
        max-size: 1g
    healthcheck:
      test: [ "CMD", "pg_isready" ]
      interval: 10s
      timeout: 5s
      retries: 5
    depends_on:
      redis:
        condition: service_healthy
        restart: true
    image: docker.local/shortthirdman/superfolio:latest
  
  superfolio-ui:
    container_name: superfolio-ui
    build:
      context: ./superfolio-ui
    expose:
      - 3000
    ports:
      - "3000:3000"
    restart: on-failure
    networks:
      - superfolio-net
    logging:
      options:
        max-size: 1g
    depends_on:
      superfolio:
        condition: service_healthy
        restart: true
    image: docker.local/shortthirdman/superfolio-ui:latest

# The commented out section below is an example of how to define a PostgreSQL
# database that your application can use. `depends_on` tells Docker Compose to
# start the database before your application. The `db-data` volume persists the
# database data between container restarts. The `db-password` secret is used
# to set the database password. You must create `db/password.txt` and add
# a password of your choosing to it before running `docker-compose up`.
#     depends_on:
#       db:
#         condition: service_healthy
#   db:
#     image: postgres
#     restart: always
#     user: postgres
#     secrets:
#       - db-password
#     volumes:
#       - db-data:/var/lib/postgresql/data
#     environment:
#       - POSTGRES_DB=example
#       - POSTGRES_PASSWORD_FILE=/run/secrets/db-password
#     expose:
#       - 5432
#     healthcheck:
#       test: [ "CMD", "pg_isready" ]
#       interval: 10s
#       timeout: 5s
#       retries: 5
# volumes:
#   db-data:
# secrets:
#   db-password:
#     file: db/password.txt
networks:
  superfolio-net:
    driver: bridge

